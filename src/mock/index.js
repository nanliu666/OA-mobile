import user from './user.js'
import message from './message.js'
import metask from './metask'
import todo from './todo'
import work from './work'
let options = { mock: false }
user(options)
message(options)
metask(options)
todo(options)
work(options)
